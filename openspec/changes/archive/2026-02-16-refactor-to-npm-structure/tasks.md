## 1. 准备工作

- [x] 1.1 备份当前项目状态（可选但推荐）
- [x] 1.2 检查 `src/` 和 `cce/src/` 目录内容，识别可能冲突的文件
- [x] 1.3 验证 `cce/src/` 目录存在且包含需要迁移的代码

## 2. 源代码迁移

- [x] 2.1 将 `cce/src/` 下的所有子目录和文件合并到根目录 `src/`
- [x] 2.2 处理任何文件冲突（如有），确保不丢失重要代码
- [x] 2.3 验证迁移后 `src/` 目录结构完整且正确

## 3. 配置文件更新

- [x] 3.1 检查并更新 `tsconfig.json` 中的源代码根目录配置
- [x] 3.2 验证 `package.json` 中的 `main` 字段路径正确
- [x] 3.3 验证 `package.json` 中的 `bin` 字段路径正确
- [x] 3.4 验证 `package.json` 中的 `scripts` 路径正确
- [x] 3.5 检查其他配置文件（如 `.eslintrc`, `.prettierrc`）是否需要更新

## 4. 清理冗余目录

- [x] 4.1 移除空的 `bin/` 目录（如果存在）
- [x] 4.2 将 `cce/README.md`（如果存在）复制到根目录并重命名或合并
- [x] 4.3 删除 `cce/` 目录
- [x] 4.4 验证根目录结构符合 npm 标准规范

## 5. 验证和测试

- [x] 5.1 运行 `npm run build` 验证构建成功
- [x] 5.2 运行 `npm run dev` 验证开发模式正常
- [x] 5.3 运行 `npm test`（如果有测试）验证测试通过
- [x] 5.4 验证 CLI 命令可以正常执行（如果有）
- [x] 5.5 检查 TypeScript 类型检查无错误

## 6. 文档更新（可选）

- [x] 6.1 更新 README.md 以反映新的项目结构
- [x] 6.2 更新任何其他文档文件中的路径引用
- [x] 6.3 提交变更到 git