---
phase: 08-source-template-webui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/webuiServer.ts
  - webui/index.html
  - webui/styles.css
  - webui/app.js
autonomous: true
requirements: [WUI-01, WUI-03, WUI-04, WUI-05, WUI-06, WUI-07, WUI-08, WUI-09]
user_setup: []

must_haves:
  truths:
    - "新建 Provider 时，Source 下拉框选项与 sourceTemplates.ts 100% 同步（经由 API 加载）"
    - "选择 Source 后，自动预填 type, baseURL, models, defaultModel 字段带绿色高亮动画"
    - "预填后显示蓝色 info toast「已应用 [Source 名称] 模板」并 2-3 秒后自动消失"
    - "用户已修改过预填字段后切换 Source，弹出确认框询问是否继续"
    - "Custom source 不留任何默认值，所有预填字段保持空白"
    - "API Key 始终不预设，强制用户手动输入"
    - "编辑模式也触发模板预填（与创建模式行为一致），覆盖 WUI-10，遵从 D-09"
    - "前端不再包含 ALL_SOURCES 和 SOURCE_DISPLAY_NAMES 硬编码"
  artifacts:
    - path: "src/core/webuiServer.ts"
      provides: "GET /api/source-templates API 端点，返回所有模板"
      min_lines: 625
      contains: "getAllTemplates"
    - path: "webui/index.html"
      provides: "Info toast 元素用于模板应用通知"
      min_lines: 342
      contains: "info-toast"
    - path: "webui/styles.css"
      provides: "模板预填绿色高亮动画和 info toast 样式"
      contains: "template-highlight"
    - path: "webui/app.js"
      provides: "动态 Source 加载、模板预填、脏字段检测、确认框、高亮动画、info toast"
      exports: []
  key_links:
    - from: "webui/app.js init()"
      to: "GET /api/source-templates"
      via: "fetch() 调用"
      pattern: "api/source-templates"
    - from: "webui/app.js provider-source change"
      to: "applySourceTemplate()"
      via: "change 事件监听器"
      pattern: "applySourceTemplate"
    - from: "webui/app.js applySourceTemplate()"
      to: ".template-filled CSS class"
      via: "classList.add('template-filled')"
      pattern: "template-filled"
    - from: "webui/app.js applySourceTemplate()"
      to: "info-toast element"
      via: "showInfo() call"
      pattern: "info-toast"
---

<objective>
将 Source Template 集成到 WebUI 的 Provider 创建/编辑表单中。

目的: WebUI 中新建/编辑 Provider 时选择 Source 后自动预填 type/baseURL/models/defaultModel 字段，与 CLI 体验一致（D-01 ~ D-11）。
输出: 
- GET /api/source-templates 后端端点
- 前端 Source 下拉框改为动态 API 加载
- 创建/编辑 Provider 时自动预填模板值 + 绿色高亮动画 + 蓝色 info toast
- Custom source 与 API Key 不预设
- 脏字段检测 + 切换确认框

冲突解决:
- WUI-02 (单模板端点) — 被 D-01/D-02 单端点 + 客户端查找方案取代，无需实现
- WUI-10 (编辑模式不预填) — 被 D-09 覆盖，编辑模式与创建模式行为一致，都触发预填
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-source-template-webui/08-CONTEXT.md
@src/core/sourceTemplates.ts
@src/core/webuiServer.ts
@src/types/index.ts
@webui/app.js
@webui/index.html
@webui/styles.css

<interfaces>
## Source Template 数据接口

Backend 返回格式 (`GET /api/source-templates`):
```typescript
// 从 sourceTemplates.ts 的 SourceTemplate 接口派生
interface SourceTemplateResponse {
  source: string;         // e.g. 'deepseek'
  displayName: string;    // e.g. 'DeepSeek'
  type: string;           // 'openai-compatible' | 'anthropic-compatible'
  baseURL: string;
  models: string[];
  defaultModel: string;
  description: string;
}

// API 响应
{
  templates: SourceTemplateResponse[];
}
```

## 后端已有相关函数

`src/core/sourceTemplates.ts`:
```typescript
export function getAllTemplates(): [SourceType, SourceTemplate][];
export function getTemplate(source: SourceType): SourceTemplate;
```

当前 `webuiServer.ts` 导入:
```typescript
import { getTemplate } from './sourceTemplates.js';
// 需要增加 getAllTemplates 导入
```

## 前端已有 DOM 元素引用（app.js 第 59-64 行）

```javascript
const elements = {
  // ...
  providerModelsInput: document.getElementById('provider-models'),
  providerDefaultModel: document.getElementById('provider-default-model'),
  providerSourceSelect: document.getElementById('provider-source'),
  providerTypeSelect: document.getElementById('provider-type'),
};
```

## 前端已有 Source 硬编码数据（app.js 第 4-24 行，需删除）

```javascript
const ALL_SOURCES = [/* 7 个硬编码选项 */];
const SOURCE_DISPLAY_NAMES = { /* 7 个硬编码映射 */ };
```

## 前端已有 toast 系统（app.js 第 67-78 行）

```javascript
function showError(message) { /* 红色 toast, 3s */ }
function showSuccess(message) { /* 绿色 toast, 3s */ }
```

需新增同模式的 `showInfo(message)`。

## 前端表单提交（app.js 第 624-697 行）

`submitProviderForm()` 收集 FormData 并验证必填字段。模板预填不改变提交逻辑——只修改表单字段值。
</interfaces>
</context>

<tasks>

<task type="auto">
<name>Task 1: 新增 GET /api/source-templates 后端端点 + info toast HTML + 高亮 CSS</name>
<files>
  - src/core/webuiServer.ts
  - webui/index.html
  - webui/styles.css
</files>
<action>
执行以下三项修改:

**1.1 webuiServer.ts — 新增 API 路由**

a) 修改第 7 行 import，增加 `getAllTemplates`:
```typescript
import { getTemplate, getAllTemplates } from './sourceTemplates.js';
```

b) 在 `app.put('/api/providers/:name', ...)` 路由之后、服务器启动之前（约第 598 行），新增路由:
```typescript
// 获取所有 Source 模板
app.get('/api/source-templates', async (req, res) => {
  try {
    const templates = getAllTemplates().map(([source, tmpl]) => ({
      source: tmpl.source,
      displayName: tmpl.displayName,
      type: tmpl.type,
      baseURL: tmpl.baseURL,
      models: tmpl.models,
      defaultModel: tmpl.defaultModel,
      description: tmpl.description,
    }));
    res.json({ templates });
  } catch (error) {
    logger.error('获取 Source 模板失败', error);
    res.status(500).json({ error: '获取 Source 模板失败' });
  }
});
```

**1.2 index.html — 新增 info toast 元素**

在成功提示 `<div id="success-toast" ...>` 后面（约第 31 行），添加:
```html
<!-- 模板应用提示 -->
<div id="info-toast" class="toast info hidden"></div>
```

**1.3 styles.css — 新增 info toast 样式 + 模板预填高亮动画**

a) 在 `.toast.error` 规则下方（约第 295 行），增加:
```css
.toast.info {
  border-left: 4px solid var(--primary);
}
```

b) 在文件末尾新增:
```css
/* Template fill highlight animation */
@keyframes template-highlight {
  0% {
    border-color: var(--success);
    box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.2);
  }
  100% {
    border-color: var(--border);
    box-shadow: none;
  }
}

.template-filled {
  animation: template-highlight 1.5s ease-out;
}
```
</action>
<verify>
  <automated>tsc --noEmit 编译通过；grep -q "getAllTemplates" src/core/webuiServer.ts && grep -q "info-toast" webui/index.html && grep -q "template-highlight" webui/styles.css</automated>
</verify>
<done>
- `GET /api/source-templates` 路由注册，返回 `{ templates: [...] }` 格式
- `tsc --noEmit` 编译通过
- `webui/index.html` 包含 `id="info-toast"` 元素
- `webui/styles.css` 包含 `.toast.info` 和 `@keyframes template-highlight` / `.template-filled`
</done>
</task>

<task type="auto">
<name>Task 2: 前端 Source 动态加载 + 模板预填逻辑（核心交互）</name>
<files>webui/app.js</files>
<action>
对 app.js 执行以下所有修改（按序逐一修改）:

**2.1 删除硬编码 + 添加新状态（约第 4-24 行）**

删除 `ALL_SOURCES` 数组和 `SOURCE_DISPLAY_NAMES` 对象（共 ~21 行）。

在 `let editingProviderName = null;` 声明后（约第 30 行），添加:
```javascript
// Source 模板数据（通过 API 动态加载）
let sourceTemplates = [];        // [{ source, displayName, type, baseURL, models, defaultModel, description }]
// 记录上次 Source 时的预填字段值，用于脏字段检测
let lastPrefilledValues = { type: '', baseURL: '', models: '', defaultModel: '' };
```

**2.2 添加 showInfo 函数（约第 78 行后）**

添加:
```javascript
const infoToast = document.getElementById('info-toast');

function showInfo(message) {
  infoToast.textContent = message;
  infoToast.classList.remove('hidden');
  setTimeout(() => infoToast.classList.add('hidden'), 2500);
}
```

**2.3 添加 loadSourceTemplates 函数**

在 `loadCurrent()` 函数之前（约第 190 行前），添加:
```javascript
// 从 API 加载 Source 模板数据
async function loadSourceTemplates() {
  try {
    const data = await apiRequest('/source-templates');
    sourceTemplates = data.templates || [];
  } catch (error) {
    console.error('加载 Source 模板失败:', error);
    sourceTemplates = [];
  }
}
```

**2.4 重写 updateProviderSourceOptions（约第 103-117 行）**

重写为:
```javascript
function updateProviderSourceOptions(selectedSource = null) {
  const sourceSelect = elements.providerSourceSelect;
  sourceSelect.innerHTML = '<option value="">请选择来源</option>';

  // 如果模板数据尚未加载，留空
  if (sourceTemplates.length === 0) return;

  sourceTemplates.forEach(tmpl => {
    const option = document.createElement('option');
    option.value = tmpl.source;
    option.textContent = tmpl.displayName;
    if (tmpl.source === selectedSource) {
      option.selected = true;
    }
    sourceSelect.appendChild(option);
  });
}
```

**2.5 修改 openProviderModal（约第 568-604 行）**

在函数开头增加 `lastPrefilledValues` 重置:
```javascript
function openProviderModal(name = null) {
  // 重置脏字段追踪
  lastPrefilledValues = { type: '', baseURL: '', models: '', defaultModel: '' };
  editingProviderName = name;
  elements.providerForm.reset();
  elements.providerDefaultModel.innerHTML = '<option value="">请先填写可用模型</option>';

  if (name) {
    // 编辑模式 — 先填充现有数据再预填（D-09: 编辑模式也触发模板预填）
    elements.providerModalTitle.textContent = '编辑 Provider';
    const provider = providers.find(p => p.name === name);
    if (provider) {
      document.getElementById('provider-name').value = provider.name;
      document.getElementById('provider-display-name').value = provider.displayName;
      document.getElementById('provider-type').value = provider.type;
      document.getElementById('provider-base-url').value = provider.baseURL;
      document.getElementById('provider-api-key').value = provider.apiKey || '';
      document.getElementById('provider-models').value = (provider.models || []).join(', ');
      updateProviderDefaultModelSelect(provider.defaultModel);
      // 更新 source 下拉框并设置选中值
      updateProviderSourceOptions(provider.source);
      document.getElementById('provider-name').disabled = true;
      // 编辑模式下 Source 变化后自动应用模板（通过已有 change 事件处理器触发）
    }
    document.getElementById('provider-api-key').removeAttribute('required');
    document.getElementById('api-key-required-mark').textContent = '';
  } else {
    // 创建模式
    elements.providerModalTitle.textContent = '新建 Provider';
    document.getElementById('provider-name').disabled = false;
    updateProviderSourceOptions();
    document.getElementById('provider-api-key').setAttribute('required', '');
    document.getElementById('api-key-required-mark').textContent = '*';
  }

  elements.providerModal.classList.remove('hidden');
}
```

**2.6 添加 applySourceTemplate 函数**

在 `openProviderModal` 下方（约第 605 行），新增:
```javascript
// 根据选中的 Source 应用模板预填
function applySourceTemplate(source) {
  if (!source || source === 'custom') {
    // Custom source — 不留任何默认值（D-07 的 Phase 7 决策）
    // 仅将 type 设为 openai-compatible
    document.getElementById('provider-type').value = 'openai-compatible';
    // 清空其他预填字段
    const fieldsToClear = ['provider-base-url', 'provider-models'];
    fieldsToClear.forEach(id => {
      const el = document.getElementById(id);
      el.value = '';
      el.closest('.form-group')?.classList.remove('template-filled');
    });
    updateProviderDefaultModelSelect();
    lastPrefilledValues = { type: 'openai-compatible', baseURL: '', models: '', defaultModel: '' };
    return;
  }

  const tmpl = sourceTemplates.find(t => t.source === source);
  if (!tmpl) return;

  // 填写预填字段
  const fills = [
    { id: 'provider-type', value: tmpl.type },
    { id: 'provider-base-url', value: tmpl.baseURL },
    { id: 'provider-models', value: tmpl.models.join(', ') },
  ];

  fills.forEach(({ id, value }) => {
    const el = document.getElementById(id);
    el.value = value;
    // 添加绿色高亮动画
    const group = el.closest('.form-group');
    if (group) {
      group.classList.remove('template-filled');
      // 触发 reflow 让动画重新播放
      void group.offsetWidth;
      group.classList.add('template-filled');
    }
  });

  // 更新默认模型下拉框
  updateProviderDefaultModelSelect(tmpl.defaultModel);

  // 记录当前预填值用于脏字段检测
  lastPrefilledValues = {
    type: tmpl.type,
    baseURL: tmpl.baseURL,
    models: tmpl.models.join(', '),
    defaultModel: tmpl.defaultModel,
  };

  // 显示 info toast（D-06）
  showInfo(`已应用 ${tmpl.displayName} 模板`);
}
```

**2.7 添加脏字段检测 + Source change 事件处理器**

在 `submitProviderForm` 函数前（约第 623 行前），新增:
```javascript
// 检测预填字段是否被用户修改过
function arePrefilledFieldsDirty() {
  const current = {
    type: document.getElementById('provider-type').value,
    baseURL: document.getElementById('provider-base-url').value,
    models: document.getElementById('provider-models').value,
    defaultModel: document.getElementById('provider-default-model').value,
  };
  return (
    current.type !== lastPrefilledValues.type ||
    current.baseURL !== lastPrefilledValues.baseURL ||
    current.models !== lastPrefilledValues.models ||
    current.defaultModel !== lastPrefilledValues.defaultModel
  );
}
```

**2.8 在 init() 中添加 Source change 事件绑定**

在 init() 函数中（约第 931 行 `// Provider 类型变化不再联动 source 下拉框` 注释下方），新增:
```javascript
  // Source 变化时自动应用模板（D-04）
  elements.providerSourceSelect.addEventListener('change', function() {
    const selectedSource = this.value;

    // 检查是否有脏字段
    if (lastPrefilledValues.type !== '' && arePrefilledFieldsDirty()) {
      // D-07: 有修改则弹出确认框
      if (!confirm('更换 Source 将重置预填字段，是否继续？')) {
        // 取消则恢复原选中值（实际不执行预填）
        return;
      }
    }

    // 应用模板预填（包括 custom 和空值两种情况）
    applySourceTemplate(selectedSource);
  });
```

**2.9 修改 renderProviders 和 showProviderDetails 中的 display name 查找**

a) 在 `renderProviders()` 中（约第 246 行），替换:
```javascript
const sourceDisplay = SOURCE_DISPLAY_NAMES[provider.source] || provider.source || '-';
```
为:
```javascript
const sourceTmpl = sourceTemplates.find(t => t.source === provider.source);
const sourceDisplay = sourceTmpl ? sourceTmpl.displayName : (provider.source || '-');
```

b) 在 `showProviderDetails()` 中（约第 512 行），同样替换:
```javascript
const sourceDisplay = SOURCE_DISPLAY_NAMES[provider.source] || provider.source || '-';
```
为:
```javascript
const sourceTmpl = sourceTemplates.find(t => t.source === provider.source);
const sourceDisplay = sourceTmpl ? sourceTmpl.displayName : (provider.source || '-');
```

**2.10 在 init() 开头增加模板加载**

在 `init()` 函数的 `Promise.all([loadProviders(), loadCurrent(), loadProfiles()])` 之前（约第 867 行），增加:
```javascript
  // 先加载 Source 模板，这样 Source 下拉框在 Provider 加载后就有数据
  loadSourceTemplates();
```
</action>
<verify>
  <automated>
  # 1. 硬编码数据已删除
  ! grep -q "ALL_SOURCES" webui/app.js && echo "PASS: ALL_SOURCES removed" || echo "FAIL: ALL_SOURCES still present"
  ! grep -q "SOURCE_DISPLAY_NAMES" webui/app.js && echo "PASS: SOURCE_DISPLAY_NAMES removed" || echo "FAIL: SOURCE_DISPLAY_NAMES still present"

  # 2. 新增关键函数/变量存在
  grep -q "sourceTemplates" webui/app.js && echo "PASS: sourceTemplates variable"
  grep -q "applySourceTemplate" webui/app.js && echo "PASS: applySourceTemplate function"
  grep -q "arePrefilledFieldsDirty" webui/app.js && echo "PASS: dirty field detection"
  grep -q "template-filled" webui/app.js && echo "PASS: .template-filled class used"
  grep -q "info-toast" webui/app.js && echo "PASS: info-toast referenced"

  # 3. 模板应用于编辑模式 (D-09)
  grep -q "编辑模式也触发模板预填" webui/app.js && echo "PASS: edit mode comment" || echo "PASS: edit mode uses same mechanism"

  # 4. API Key 无预填
  grep -q "provider-api-key" webui/app.js | grep -v "lastPrefilledValues" && echo "PASS: apiKey reference exists" || echo "NOTE: checking apiKey safety"
  grep "applySourceTemplate" webui/app.js | grep -q "api-key" && echo "FAIL: applySourceTemplate touches apiKey!" || echo "PASS: applySourceTemplate does NOT touch apiKey"

  # 5. Custom source 无预填
  grep -A10 "source === 'custom'" webui/app.js | grep -q "type.*openai-compatible" && echo "PASS: custom only sets type"

  # 6. 类型检查
  tsc --noEmit && echo "PASS: tsc --noEmit"
  </automated>
</verify>
<done>
- `ALL_SOURCES` 和 `SOURCE_DISPLAY_NAMES` 硬编码已完全移除，source 下拉框通过 `GET /api/source-templates` 动态加载
- 选择/切换 Source 后 2-3 个预填字段 (type/baseURL/models/defaultModel) 自动填入 + 绿色边框闪烁动画
- 蓝色 info toast 显示「已应用 [Source 名称] 模板」，2.5 秒后自动消失
- 用户修改预填字段后切换 Source，弹出 `confirm()` 确认框
- Custom source 仅设 type 为 openai-compatible，其他预填字段全部为空
- API Key 从未被模板触及，始终留空由用户输入
- 编辑模式打开 Provider 表单时也应用模板预填（先填现有值，Source change 触发预填）
- `tsc --noEmit` 编译通过
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> API | 用户在前端操作触发 API 调用，传入 source 值 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Spoofing | GET /api/source-templates | accept | 返回公开模板数据（baseURL、模型列表），无认证要求。运行在 localhost，仅本地访问 |
| T-08-02 | Information Disclosure | GET /api/source-templates | accept | API 只返回模板定义（baseURL 和模型列表均为公开信息），不泄露任何用户凭据或密钥 |
</threat_model>

<verification>
- `tsc --noEmit` 编译通过
- `webui/app.js` 中 `ALL_SOURCES` 和 `SOURCE_DISPLAY_NAMES` 引用计数为 0
- `webui/app.js` 中包含 `applySourceTemplate`、`arePrefilledFieldsDirty`、`showInfo` 函数定义
- `src/core/webuiServer.ts` 中包含 `getAllTemplates` 调用
- `webui/index.html` 包含 `info-toast` 元素
- `webui/styles.css` 包含 `@keyframes template-highlight` 和 `.template-filled` 样式
</verification>

<success_criteria>
1. 后端: `GET /api/source-templates` 返回 7 个模板，包含 source/displayName/type/baseURL/models/defaultModel/description
2. 前端: Provider 表单的 Source 下拉框选项与 API 返回数据一致（7 个选项）
3. 选择 DeepSeek -> type=openai-compatible, baseURL=https://api.deepseek.com, models 填入两模型, defaultModel 选中 deepseek-chat
4. 预填字段出现绿色边框闪烁动画，同时显示蓝色 info toast，2.5 秒后消失
5. 手动修改预填字段后切换 Source -> `confirm()` 弹出 -> 确认则重填 -> 取消则保持不变
6. 选择 Custom -> 仅 type 变为 openai-compatible，其他字段全部清空
7. API Key 始终空白
8. 编辑模式打开已有 Provider -> 选择新的 Source 同样触发预填
9. 前端代码中不再有 `ALL_SOURCES` 或 `SOURCE_DISPLAY_NAMES`
</success_criteria>

<output>
After completion, create `.planning/phases/08-source-template-webui/08-01-SUMMARY.md`
</output>
