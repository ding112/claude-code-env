---
phase: 01-type-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02
  - TYPE-03

must_haves:
  truths:
    - "SourceType 类型存在且值与原 VendorType 完全一致"
    - "VendorType 类型不再存在于代码中"
    - "Provider 接口包含 source?: SourceType 字段，不含 vendor 字段"
    - "EffectiveConfig 接口包含 source?: SourceType 字段，不含 vendor 字段"
  artifacts:
    - path: "src/types/index.ts"
      provides: "重命名后的类型定义"
      contains: "SourceType"
  key_links:
    - from: "src/types/index.ts"
      to: "SourceType"
      via: "export type SourceType"
      pattern: "export type SourceType"
    - from: "src/types/index.ts"
      to: "Provider.source"
      via: "接口字段定义"
      pattern: "source\\?: SourceType"
---

<objective>
将 src/types/index.ts 中所有 vendor 相关类型命名统一改为 source 体系。

Purpose: 为后续 Phase 2-5 的核心层、CLI 层、WebUI 层重命名提供类型基础。被依赖方先改，依赖方后改，TypeScript 编译器可在后续阶段精确定位遗漏。
Output: 修改后的 src/types/index.ts，包含 SourceType、Provider.source、EffectiveConfig.source
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重命名 VendorType 为 SourceType，Provider.vendor 和 EffectiveConfig.vendor 为 source</name>
  <files>src/types/index.ts</files>
  <read_first>src/types/index.ts</read_first>
  <action>
在 src/types/index.ts 中执行以下三处修改：

1. **TYPE-01 — 重命名 VendorType 为 SourceType**（第 8-16 行区域）：
   - 将 `// Vendor 类型（供应商标识）` 注释改为 `// Source 类型（配置来源标识）`
   - 将 `export type VendorType =` 改为 `export type SourceType =`
   - 保留所有联合值不变：`'deepseek' | 'volcengine' | 'tencent' | 'alibaba' | 'openai' | 'anthropic' | 'custom'`

2. **TYPE-02 — 重命名 Provider.vendor 为 Provider.source**（第 23 行区域）：
   - 将 `vendor?: VendorType;` 改为 `source?: SourceType;`
   - 将 `// 供应商标识（可选）` 注释改为 `// 配置来源标识（可选）`

3. **TYPE-03 — 重命名 EffectiveConfig.vendor 为 EffectiveConfig.source**（第 83 行区域）：
   - 将 `vendor?: VendorType;` 改为 `source?: SourceType;`

不修改其他任何内容。不修改其他文件。

**预期副作用**：修改完成后，`tsc --noEmit` 会报编译错误，因为其他文件（如 src/core/provider.ts、src/core/switch.ts、src/commands/provider.ts 等）仍然引用 `VendorType`。这是预期行为，这些错误将在 Phase 2-3 中修复。
  </action>
  <verify>
    <automated>grep -c "VendorType" src/types/index.ts && echo "FAIL: VendorType still exists" || echo "PASS: VendorType removed"</automated>
    <automated>grep -c "SourceType" src/types/index.ts</automated>
    <automated>grep -c "vendor" src/types/index.ts && echo "FAIL: vendor still exists" || echo "PASS: vendor removed"</automated>
    <automated>grep -c "source" src/types/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/types/index.ts 中不包含字符串 "VendorType"
    - src/types/index.ts 中不包含字符串 "vendor"（小写，作为字段名）
    - src/types/index.ts 包含 "export type SourceType"
    - src/types/index.ts 包含 "source?: SourceType" 且出现 2 次（Provider 和 EffectiveConfig 各一次）
    - src/types/index.ts 中 SourceType 的联合值与原 VendorType 完全一致：'deepseek' | 'volcengine' | 'tencent' | 'alibaba' | 'openai' | 'anthropic' | 'custom'
    - 文件其他部分（ProviderType、Profile、ClaudeEnvConfig 等）未做任何修改
  </acceptance_criteria>
  <done>
    - VendorType 已重命名为 SourceType，联合值不变
    - Provider.vendor?: VendorType 已改为 Provider.source?: SourceType
    - EffectiveConfig.vendor?: VendorType 已改为 EffectiveConfig.source?: SourceType
    - src/types/index.ts 中不含 VendorType 或 vendor 字段引用
    - TypeScript 编译报错（其他文件引用 VendorType）为预期行为，不影响本任务完成
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (无) | 本阶段仅修改类型定义，不涉及运行时行为、外部输入或信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | - | - | accept | 纯类型重命名，无安全影响 |
</threat_model>

<verification>
1. `grep -c "VendorType" src/types/index.ts` 返回 0
2. `grep -c "vendor" src/types/index.ts` 返回 0（排除注释中的 "供应商" 也不应存在）
3. `grep "export type SourceType" src/types/index.ts` 匹配成功
4. `grep -c "source?: SourceType" src/types/index.ts` 返回 2
5. `grep "SourceType" src/types/index.ts` 输出中包含完整联合值
</verification>

<success_criteria>
1. TypeScript 编译器可识别 `SourceType` 类型（存在于 src/types/index.ts 导出中）
2. `VendorType` 不再存在于 src/types/index.ts
3. Provider 接口包含 `source?: SourceType` 字段，不含 `vendor` 字段
4. EffectiveConfig 接口包含 `source?: SourceType` 字段，不含 `vendor` 字段
5. 其他文件引用 VendorType 导致编译错误为预期行为
</success_criteria>

<output>
After completion, create `.planning/phases/01-type-rename/01-01-SUMMARY.md`
</output>
